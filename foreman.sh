Configure temp foreman to build our infra structure

Pulls in the puppet forge module.

Then we use hammer scripts to build hosts.

The config in here will be dumped in db and moved to the real foreman this hosts builds.
